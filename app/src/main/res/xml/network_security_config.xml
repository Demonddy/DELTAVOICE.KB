<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!--
    Certificate pinning for production API hosts.
    Uncomment and replace pins before production release.

    Generate pins with:
      openssl s_client -servername DOMAIN -connect DOMAIN:443 2>/dev/null | \
        openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | \
        openssl dgst -sha256 -binary | openssl enc -base64

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">rkfveqzktfmgegtsoxlf.supabase.co</domain>
        <domain includeSubdomains="true">kindred-curlew-363.eu-west-1.convex.site</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">YOUR_BASE64_PIN_HERE=</pin>
            <pin digest="SHA-256">YOUR_BACKUP_PIN_HERE=</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>
