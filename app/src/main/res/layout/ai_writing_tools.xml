<?xml version="1.0" encoding="utf-8"?>
<!-- IME only: tool grid, no text field (overlay uses ai_writing_tools_overlay) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ai_writing_tools_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/ai_chat_panel_rounded_28"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:layout_marginBottom="6dp">

        <ImageButton
            android:id="@+id/ai_tools_close_btn"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/content_desc_close_ai_tools"
            android:tint="#9CA3AF" />
    </LinearLayout>

    <include layout="@layout/ai_writing_tools_content" />

</LinearLayout>
